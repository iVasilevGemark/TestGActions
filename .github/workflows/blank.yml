name: ProductX CI/CD

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  ktlint:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: ScaCap/action-ktlint@master